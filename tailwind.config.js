// tailwind.config.js
export const content = [
  "./src/**/*.{js,jsx,ts,tsx}", // Make sure the content paths are correct
];
export const theme = {
  extend: {},
};
export const darkMode = 'class';
export const plugins = [];
